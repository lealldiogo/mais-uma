
<div class="container">
  <div class="sector-select-title">
    <h3>Em qual setor você se encontra?</h3>
  </div>
  <div class="row">
    <div class="panel panel-default">
      <div class="panel-body">
        <div class="list-group stadium-sectors col-md-6 col-sm-12" style="margin-top: 20px; ">
          <p><b>Partida:</b> <%= @event.home_team %> x <%= @event.away_team %></p>
          <p><b>Data:</b> <%= @event.datetime.day %>/<%= @event.datetime.month %>/<%= @event.datetime.year %></p>
          <p><b>Horário:</b> <%= @event.datetime.hour %>:00</p>
          <p><b>Local:</b> <%= @event.location %></p>

      <!-- <button type="button" class="btn btn-default button-sectors">Default</button> -->
        </div>
        <div class=" col-xs-12 col-md-3 col-sm-12">
          <div class="edit-form" style="width: 100px;">
            <% @sections.each do |sec| %>
              <% if sec.id < 5 %>
                <%= link_to products_select_path(section: sec.id) do %>
                  <div class="list-group-item" style="width: 200px; height: 40px; text-align: center">
                    <p><%= sec.name %></p>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class=" col-xs-12 col-md-3 col-sm-12">
          <div class="edit-form" style="width: 100px;">
            <% @sections.each do |sec| %>
              <% if sec.id > 4 %>
                <%= link_to products_select_path(section: sec.id) do %>
                  <div class="list-group-item" style="width: 200px; height: 40px; text-align: center">
                    <p><%= sec.name %></p>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          </div>
        </div>

      </div>
    </div>
    <div class="col-xs-12 col-md-6 col-sm-12">
      <div class="stadium-img">
        <%= cl_image_tag(@event.photo, class: "img-responsive",  crop: :fill)%>
      </div>
    </div>

  </div>
</div>





<script>

</script>



<%# @sections.each do |sec| %>
          <%#= link_to products_select_path(section: sec.id), class: "list-group-item" do %>
            <!-- <button class="button-hidden"  type="button" class="list-group-item"> -->
              <%#= sec.name %>
            <!-- </button> -->
          <%# end %>
        <%# end %>


        <%#= form_tag(products_select_path(@section), method: "get") do %>
          <%#= select_tag(:section, options_from_collection_for_select(@event.sections, "id", "name"), prompt: "Selecione seu setor", class: 'btn btn-primary btn-lg') %>
          <%#= submit_tag 'Save' %>
        <%# end %>


