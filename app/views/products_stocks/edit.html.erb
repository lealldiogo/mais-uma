
<div class="container">
  <div class="row">
    <div class="col-sm-4 col-sm-offset-4">
      <%= simple_form_for [@product, @products_stock] do |f| %>
        <% #,collection: ProductsStock.stands %>
        <%= f.input :stand, disabled: true %>
        <%= f.input :quantity %>
        <%= f.button :submit %>
        <%= link_to "Cancelar", products_path %>
      <% end %>
    </div>
  </div>
</div>
